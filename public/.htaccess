AddDefaultCharset UTF-8

RewriteEngine On

# Les fichiers réels restent servis directement; le reste passe par le routeur MVC.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]

